// import React, { useContext } from "react";
// import { CounterContextB } from "../App";

// export default function CounterButton() {
//   const { setCount } = useContext(CounterContextB);
//   return <button onClick={() => setCount((prev) => prev + 1)}> + </button>;
// }